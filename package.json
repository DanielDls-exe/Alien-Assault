{
  "name": "space-assault",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "yarn vite",
    "build": "yarn tsc && yarn vite build",
    "preview": "yarn vite preview",
    "start": "yarn dev",
    "deploy": "yarn build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "typescript": "^5.0.2",
    "vite": "^4.3.2"
  },
  "dependencies": {
    "sweetalert2": "^11.7.11"
  }
}
